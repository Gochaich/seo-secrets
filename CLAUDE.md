@AGENTS.md

# Сайт агентства SEO Secrets

Все правила проекта — в `AGENTS.md`, он подключён строкой выше и уже прочитан.
Второго списка правил здесь нет намеренно: над проектом работают два агента
(Claude Code и Codex), Codex читает `AGENTS.md` напрямую, и два файла с
правилами разошлись бы на первой же правке.

Новое правило пишем в `AGENTS.md`, а не сюда.
